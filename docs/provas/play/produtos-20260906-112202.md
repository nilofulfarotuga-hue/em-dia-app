# Prova — subscrições da Play (2026-09-06 11:21)

Modo: **CRIAR (planos base em DRAFT)**

- `subscriptions.list` → 0 já existem: (nenhuma)
- ERRO `pro_mensal`: POST applications/pt.emdia.app/subscriptions?productId=pro_mensal&regionsVersion.version=2022/0 -> HTTP 400: {
  "error": {
    "code": 400,
    "message": "Cannot create a subscription without first registering a payments profile for the developer account.",
    "status": "FAILED_PRECONDITION"
  }
}

- ERRO `pro_anual`: POST applications/pt.emdia.app/subscriptions?productId=pro_anual&regionsVersion.version=2022/02 -> HTTP 400: {
  "error": {
    "code": 400,
    "message": "Cannot create a subscription without first registering a payments profile for the developer account.",
    "status": "FAILED_PRECONDITION"
  }
}

- ERRO `familia_mensal`: POST applications/pt.emdia.app/subscriptions?productId=familia_mensal&regionsVersion.version=20 -> HTTP 400: {
  "error": {
    "code": 400,
    "message": "Cannot create a subscription without first registering a payments profile for the developer account.",
    "status": "FAILED_PRECONDITION"
  }
}

- ERRO `familia_anual`: POST applications/pt.emdia.app/subscriptions?productId=familia_anual&regionsVersion.version=202 -> HTTP 400: {
  "error": {
    "code": 400,
    "message": "Cannot create a subscription without first registering a payments profile for the developer account.",
    "status": "FAILED_PRECONDITION"
  }
}

