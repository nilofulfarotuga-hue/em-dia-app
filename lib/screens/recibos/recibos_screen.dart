import 'package:flutter/material.dart';

/// Recibos — ESQUELETO. Substituído pelo ecrã real no bloco 2.
class RecibosScreen extends StatelessWidget {
  const RecibosScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Recibos')),
      body: const Center(child: Text('Recibos — em construção')),
    );
  }
}
